import { createTheme} from '@mui/material/styles';
export const redTheme = createTheme({
    palette: {
        primary: {
            main: '#961313'
        },
        secondary: {
            main: '#e82f2f'
        },
    },
});